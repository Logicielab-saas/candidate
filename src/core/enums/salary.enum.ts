export enum SalaryDisplayType {
  RANGE = "range",
  FIXED = "fixed",
  NEGOTIABLE = "negotiable",
}

export enum SalaryFrequency {
  HOURLY = "hourly",
  DAILY = "daily",
  WEEKLY = "weekly",
  MONTHLY = "monthly",
  YEARLY = "yearly",
}
